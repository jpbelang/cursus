import 'mocha'

describe('wil this work', function() {
    this.timeout(5000);

    before(function () {
    })

    after(function () {

    })

    it("can subscribe", async () => {

        console.log("happy")
    })
    }
)